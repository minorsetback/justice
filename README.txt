1) Install Docker.
2) Open Command Line.
3) Open in Command Line the place where you have unpacked archive(use commands 'cd' and 'dir' to navigate)
4) Run those two commands to Command Line:

	docker build -t justice:latest .
	docker run justice